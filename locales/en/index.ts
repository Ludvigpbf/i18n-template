// Import translations from common.ts and add more translations here
import common from "./common.json";

// To use ts types, uncomment the following line and comment/delete the previous one
// import commonEn from "./common";

const enTranslation = {
  ...common,
  // To use the .ts file, uncomment the following line and comment/delete the previous one
  // ...commonEn,

  // add more translations here
};

export default enTranslation;
